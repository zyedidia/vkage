This is the Linux UML tree for VKage. This holds the source code for the Linux
guest that runs drivers in userspace via UML. This tree contains significant
modifications to UML, including support for "no-userspace", arm64, mmap'd BARs,
and lazy DMA mapping. Upstream UML also already includes support for SMP.
